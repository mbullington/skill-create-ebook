[project]
name = "skill-create-ebook"
version = "0.1.0"
description = "Bundled tools for an agent skill that creates unofficial public-domain EPUBs"
requires-python = ">=3.12"
dependencies = [
    "lxml==6.1.1",
    "standardebooks==4.0.3",
]
